with-expecter: True
dir: mocks/{{ replaceAll .InterfaceDirRelative "internal" "internal_" }}
mockname: "{{.InterfaceName}}"
outpkg: "{{.PackageName}}"
filename: "{{.InterfaceName}}.go"
all: True
packages:
  gophkeeper.com/internal/server:
    config:
      recursive: True
  gophkeeper.com/pkg/generated/api/proto/v1:
    config:
      recursive: True
